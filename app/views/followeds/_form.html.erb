<%= form_with(model: followed) do |form| %>
  <% if followed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(followed.errors.count, "error") %> prohibited this followed from being saved:</h2>

      <ul>
        <% followed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :followed_user_id, style: "display: block" %>
    <%= form.text_field :followed_user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
